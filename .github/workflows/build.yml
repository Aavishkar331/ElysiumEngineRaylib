name: Build and Test

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]
  workflow_dispatch:

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: Linux
            preset: linux
          - os: macos-latest
            name: macOS
            preset: macos
          - os: windows-latest
            name: Windows
            preset: windows

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libxrandr-dev \
            libxcursor-dev \
            libxi-dev \
            libxinerama-dev \
            libgl1-mesa-dev \
            libglu1-mesa-dev \
            xorg-dev

      - name: Configure CMake
        shell: bash
        run: |
          cmake -B build \
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }}

      - name: Build
        shell: bash
        working-directory: build
        run: cmake --build . --config ${{ env.BUILD_TYPE }} --parallel 2

      - name: Run tests (Linux/macOS)
        if: runner.os != 'Windows'
        working-directory: build
        run: |
          # Make executables executable
          chmod +x PhysicsPerformanceTest BroadPhasePerformanceTest BoundaryTest FrictionTest 2>/dev/null || true
          # Run tests
          ./PhysicsPerformanceTest
          ./BroadPhasePerformanceTest
          ./BoundaryTest
          ./FrictionTest

      - name: Run tests (Windows)
        if: runner.os == 'Windows'
        working-directory: build
        run: |
          ./Release/PhysicsPerformanceTest.exe
          ./Release/BroadPhasePerformanceTest.exe
          ./Release/BoundaryTest.exe
          ./Release/FrictionTest.exe

      - name: Upload Linux artifacts
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: ElysiumEngine-Linux
          path: |
            build/ElysiumApp
            build/PhysicsPerformanceTest
            build/BroadPhasePerformanceTest
            build/BoundaryTest
            build/FrictionTest
          retention-days: 7

      - name: Upload macOS artifacts
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: ElysiumEngine-macOS
          path: |
            build/ElysiumApp
            build/PhysicsPerformanceTest
            build/BroadPhasePerformanceTest
            build/BoundaryTest
            build/FrictionTest
          retention-days: 7

      - name: Upload Windows artifacts
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ElysiumEngine-Windows
          path: |
            build/Release/ElysiumApp.exe
            build/Release/PhysicsPerformanceTest.exe
            build/Release/BroadPhasePerformanceTest.exe
            build/Release/BoundaryTest.exe
            build/Release/FrictionTest.exe
          retention-days: 7
